document.addEventListener('DOMContentLoaded',()=>{const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);const loader=$('#loader'),menu=$('.menu'),nav=$('nav');if(menu&&nav)menu.addEventListener('click',()=>{nav.classList.toggle('open');menu.setAttribute('aria-expanded',nav.classList.contains('open'))});if(loader){setTimeout(()=>loader.classList.add('is-hidden'),700);setTimeout(()=>loader.remove(),1600)}const progress=document.createElement('div');progress.id='scroll-progress';document.body.append(progress);const onScroll=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max>0?scrollY/max*100:0)+'%';document.querySelector('header')?.classList.toggle('scrolled',scrollY>40)};addEventListener('scroll',onScroll,{passive:true});onScroll();const items=$$('.reveal');const io=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');entry.target.classList.remove('exit')}else{entry.target.classList.remove('visible');if(entry.boundingClientRect.top<innerHeight)entry.target.classList.add('exit')}}),{threshold:.12,rootMargin:'-8% 0px -8% 0px'});items.forEach((el,i)=>{el.style.transitionDelay=(i%5)*60+'ms';io.observe(el)});if(matchMedia('(hover:hover)').matches){const glow=document.createElement('div');glow.id='cursor-glow';document.body.append(glow);let tx=innerWidth/2,ty=innerHeight/2,x=tx,y=ty;addEventListener('pointermove',e=>{tx=e.clientX;ty=e.clientY});const tick=()=>{x+=(tx-x)*.1;y+=(ty-y)*.1;glow.style.left=x+'px';glow.style.top=y+'px';requestAnimationFrame(tick)};tick();$$('.tilt').forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),rx=((e.clientY-r.top)/r.height-.5)*-4,ry=((e.clientX-r.left)/r.width-.5)*5;card.style.transform=`perspective(900px) rotateX(${rx}deg) rotateY(${ry}deg) translateY(-8px)`});card.addEventListener('pointerleave',()=>card.style.transform='')})}$$('a[href*="wa.me"]').forEach(a=>a.setAttribute('aria-label','Book via WhatsApp'));$$('a[href^="tel:"]').forEach(a=>a.setAttribute('aria-label','Call Hanh Beauty SG'))});